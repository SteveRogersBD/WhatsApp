<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.WhatsApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Customize other theme attributes as needed -->
    </style>

    <!-- WhatsApp theme -->
    <style name="Theme.WhatsApp" parent="Base.Theme.WhatsApp">
        <!-- Add WhatsApp-specific customizations here -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <!-- Customize other theme attributes as needed -->
    </style>

</resources>
